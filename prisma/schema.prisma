generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================================================
// PLATFORM MODELS (Super Admin level)
// ============================================================================

model PlatformAdmin {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  name         String
  role         String   @default("admin") // "admin", "superadmin"
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([email])
  @@map("platform_admin")
}

model Plan {
  id              String   @id @default(cuid())
  name            String   @unique // "Free", "Starter", "Pro", "Enterprise"
  slug            String   @unique // "free", "starter", "pro", "enterprise"
  description     String?
  price           Int      @default(0) // in cents (e.g., 9900 = $99.00)
  billingInterval String   @default("month") // "month", "year"
  features        Json // Array of features (legacy, being replaced by PlanFeature)
  limits          Json // Resource limits: { menuItems: 100, tables: 10, orders: 1000 }
  isActive        Boolean  @default(true)
  sortOrder       Int      @default(0)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  subscriptions Subscription[]
  planFeatures  PlanFeature[]

  @@index([slug])
  @@index([isActive])
  @@map("plan")
}

// Feature Category - organize features into categories
model FeatureCategory {
  id          String   @id @default(cuid())
  code        String   @unique // "branding", "analytics", "integrations"
  name        String // "Branding", "Analytics"
  description String?
  icon        String? // Icon class/name for UI
  isActive    Boolean  @default(true)
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  features Feature[]

  @@index([code])
  @@index([sortOrder])
  @@map("feature_category")
}

// Feature catalog - defines available features across the platform
model Feature {
  id          String   @id @default(cuid())
  code        String   @unique // "CUSTOM_DOMAIN", "ADVANCED_ANALYTICS", "API_ACCESS"
  name        String // "Custom Domain", "Advanced Analytics"
  description String?
  categoryId  String? // Reference to FeatureCategory
  isActive    Boolean  @default(true)
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  category        FeatureCategory?        @relation(fields: [categoryId], references: [id], onDelete: SetNull)
  planFeatures    PlanFeature[]
  tenantOverrides TenantFeatureOverride[]

  @@index([code])
  @@index([categoryId])
  @@map("feature")
}

// Plan-Feature mapping - which features are available in each plan
model PlanFeature {
  planId    String
  featureId String
  enabled   Boolean  @default(true)
  createdAt DateTime @default(now())

  plan    Plan    @relation(fields: [planId], references: [id], onDelete: Cascade)
  feature Feature @relation(fields: [featureId], references: [id], onDelete: Cascade)

  @@id([planId, featureId])
  @@index([planId])
  @@index([featureId])
  @@map("plan_feature")
}

// Tenant-specific feature overrides - allow/deny specific features per tenant
model TenantFeatureOverride {
  tenantId  String
  featureId String
  enabled   Boolean
  reason    String? // Optional: why this override exists
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant  Tenant  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  feature Feature @relation(fields: [featureId], references: [id], onDelete: Cascade)

  @@id([tenantId, featureId])
  @@index([tenantId])
  @@index([featureId])
  @@map("tenant_feature_override")
}

enum SubscriptionStatus {
  TRIAL
  ACTIVE
  PAST_DUE
  CANCELLED
  EXPIRED
}

model Subscription {
  id                 String             @id @default(cuid())
  tenantId           String             @unique
  plan               String? // Legacy: "free", "starter", "pro", "enterprise" - nullable for migration
  planId             String? // FK to Plan - nullable for migration
  status             SubscriptionStatus @default(TRIAL)
  currentPeriodStart DateTime?
  currentPeriodEnd   DateTime?
  trialEndsAt        DateTime?
  cancelledAt        DateTime?
  metadata           Json? // Payment gateway info, etc
  createdAt          DateTime           @default(now())
  updatedAt          DateTime           @updatedAt

  tenant       Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  planRelation Plan?  @relation(fields: [planId], references: [id], onDelete: Restrict)

  @@index([tenantId])
  @@index([planId])
  @@index([status])
  @@map("subscription")
}

// ============================================================================
// BASE MODELS (Shared across all business types)
// ============================================================================

model Tenant {
  id           String   @id @default(cuid())
  slug         String   @unique // untuk subdomain/URL: warungpak.yourapp.com
  name         String
  businessType String // "restaurant", "retail", "salon"
  ownerEmail   String   @default("") // Email pemilik tenant
  ownerName    String   @default("") // Nama pemilik tenant
  plan         String   @default("free") // "free", "starter", "pro"
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  // Relations
  subscription   Subscription?
  adminUsers     AdminUser[]
  users          User[]
  auditLogs      AuditLog[]
  siteSettings   SiteSettings[]
  systemSettings SystemSettings?
  landingHeroes  LandingHero[]
  campaigns      Campaign[]
  reviews        Review[]
  businessInfo   BusinessInfo?
  themeConfig    ThemeConfig?

  // Restaurant module relations
  categories   Category[]
  menuItems    MenuItem[]
  tables       Table[]
  reservations Reservation[]
  orders       Order[]
  payments     Payment[]

  // RBAC relations
  tenantPermissionOverrides TenantPermissionOverride[]

  // Feature gating
  tenantFeatureOverrides TenantFeatureOverride[]

  @@index([ownerEmail])
  @@map("tenant")
}

model BusinessInfo {
  id           String   @id @default(cuid())
  tenantId     String   @unique
  name         String
  description  String?
  address      String?
  phoneNumber  String?
  email        String?
  mapsUrl      String?
  mapsEmbedUrl String?
  metadata     Json? // Module-specific fields (openingHours, etc)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("business_info")
}

model ThemeConfig {
  id             String   @id @default(cuid())
  tenantId       String   @unique
  primaryColor   String   @default("#16a34a")
  secondaryColor String   @default("#ca8a04")
  fontFamily     String   @default("Inter")
  layoutVariant  String   @default("default") // "default", "minimal", "bold"
  customCss      String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("theme_config")
}

model AdminUser {
  id           String   @id @default(cuid())
  tenantId     String
  email        String
  passwordHash String
  name         String
  role         String   @default("OWNER") // OWNER, MANAGER, CASHIER, WAITER, KITCHEN
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, email])
  @@index([email])
  @@map("admin_user")
}

enum OrderType {
  DINE_IN
  TAKEAWAY
}

enum OrderStatus {
  PLACED
  ACCEPTED
  IN_KITCHEN
  READY
  COMPLETED
  CANCELLED
}

enum ReservationStatus {
  PENDING
  CONFIRMED
  CANCELLED
  NO_SHOW
  COMPLETED
}

enum PaymentProvider {
  MIDTRANS
  CASH
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  EXPIRED
  REFUNDED
}

enum UserRole {
  OWNER // Full access to tenant
  MANAGER // Manage operations (orders, menu, staff)
  CASHIER // POS and payment operations
  WAITER // Create and manage orders
  KITCHEN // Kitchen display and order status
  CUSTOMER // Public user (place orders, reviews, reservations)
}

enum AuditAction {
  CREATE
  UPDATE
  DELETE
  LOGIN
  LOGOUT
  APPROVE
  REJECT
  CANCEL
}

model AuditLog {
  id        String      @id @default(cuid())
  tenantId  String // Which tenant this belongs to
  userId    String? // Who performed the action (null for system actions)
  action    AuditAction // What action was performed
  entity    String // What entity was affected (Order, Menu, User, etc)
  entityId  String? // ID of the affected entity
  oldData   Json? // Previous state (for UPDATE/DELETE)
  newData   Json? // New state (for CREATE/UPDATE)
  metadata  Json? // Additional context (IP, user agent, etc)
  createdAt DateTime    @default(now())

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user   User?  @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([userId])
  @@index([entity])
  @@index([action])
  @@index([createdAt])
  @@map("audit_log")
}

model User {
  id           String   @id @default(cuid())
  tenantId     String? // null for platform-level customers (cross-tenant)
  email        String
  passwordHash String? // null for guest orders
  name         String
  phoneNumber  String?
  role         UserRole @default(CUSTOMER)
  isActive     Boolean  @default(true)
  metadata     Json? // Role-specific settings, preferences
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant       Tenant?       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  orders       Order[]       @relation("OrderCreatedBy")
  reservations Reservation[] @relation("ReservationCreatedBy")
  reviews      Review[]      @relation("ReviewAuthor")
  auditLogs    AuditLog[]

  @@unique([tenantId, email])
  @@index([email])
  @@index([tenantId])
  @@index([role])
  @@map("user")
}

// ============================================================================
// BUSINESS MODULE (generic tables for all business types)
// ============================================================================

model Category {
  id        String   @id @default(cuid())
  tenantId  String
  name      String
  slug      String
  sortOrder Int      @default(0)
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  items  MenuItem[]

  @@unique([tenantId, slug])
  @@index([tenantId])
  @@map("category")
}

model MenuItem {
  id          String   @id @default(cuid())
  tenantId    String
  categoryId  String?
  name        String
  description String?
  price       Int
  photoUrl    String?
  isAvailable Boolean  @default(true)
  isFeatured  Boolean  @default(false)
  sortOrder   Int      @default(0)
  stock       Int? // null = unlimited stock, 0 = out of stock
  sku         String? // Stock Keeping Unit
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  tenant     Tenant      @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  category   Category?   @relation(fields: [categoryId], references: [id])
  orderItems OrderItem[]

  @@index([tenantId])
  @@index([categoryId])
  @@index([isFeatured])
  @@map("item")
}

model Table {
  id        String   @id @default(cuid())
  tenantId  String
  tableCode String
  name      String?
  capacity  Int
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant       Tenant        @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  reservations Reservation[]
  orders       Order[]

  @@unique([tenantId, tableCode])
  @@index([tenantId])
  @@map("resource_table")
}

model Reservation {
  id            String            @id @default(cuid())
  tenantId      String
  tableId       String?
  userId        String? // Customer who made the reservation (optional for guest)
  customerName  String
  customerPhone String
  partySize     Int
  startAt       DateTime
  notes         String?
  status        ReservationStatus @default(PENDING)
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt

  tenant Tenant  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  table  Table?  @relation(fields: [tableId], references: [id])
  user   User?   @relation("ReservationCreatedBy", fields: [userId], references: [id])
  orders Order[]

  @@index([tenantId])
  @@index([tableId])
  @@index([userId])
  @@index([status])
  @@index([startAt])
  @@map("reservation")
}

model Order {
  id          String      @id @default(cuid())
  tenantId    String
  orderNumber String
  type        OrderType
  status      OrderStatus @default(PLACED)

  tableId       String?
  reservationId String?
  userId        String? // Customer who placed the order (optional for guest)
  createdById   String? // Staff who created/input the order (for staff-assisted orders)

  customerName  String?
  customerPhone String?
  notes         String?

  subtotal Int @default(0)
  total    Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant      Tenant       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  table       Table?       @relation(fields: [tableId], references: [id])
  reservation Reservation? @relation(fields: [reservationId], references: [id])
  user        User?        @relation("OrderCreatedBy", fields: [userId], references: [id])
  items       OrderItem[]
  payments    Payment[]

  @@unique([tenantId, orderNumber])
  @@index([tenantId])
  @@index([tableId])
  @@index([reservationId])
  @@index([userId])
  @@index([status])
  @@index([type])
  @@map("order")
}

model OrderItem {
  id         String @id @default(cuid())
  orderId    String
  menuItemId String

  nameSnapshot  String
  priceSnapshot Int
  qty           Int
  notes         String?
  lineTotal     Int

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  order    Order    @relation(fields: [orderId], references: [id], onDelete: Cascade)
  menuItem MenuItem @relation(fields: [menuItemId], references: [id])

  @@index([orderId])
  @@index([menuItemId])
  @@map("order_item")
}

model Payment {
  id       String          @id @default(cuid())
  tenantId String
  orderId  String
  provider PaymentProvider @default(MIDTRANS)
  amount   Int
  currency String          @default("IDR")
  status   PaymentStatus   @default(PENDING)

  providerRef     String?
  providerPayload Json?
  paidAt          DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  order  Order  @relation(fields: [orderId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@index([orderId])
  @@index([status])
  @@map("payment")
}

// ============================================================================
// CMS / LANDING PAGE MODELS (Base with tenantId)
// ============================================================================

model Review {
  id          String   @id @default(cuid())
  tenantId    String
  userId      String? // User who wrote the review (null for anonymous)
  authorName  String
  rating      Int
  comment     String?
  isPublished Boolean  @default(false)
  isFeatured  Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user   User?  @relation("ReviewAuthor", fields: [userId], references: [id])

  @@index([tenantId])
  @@index([userId])
  @@index([isPublished])
  @@index([isFeatured])
  @@index([rating])
  @@map("review")
}

enum CampaignType {
  DISCOUNT
  POINTS
  BUNDLE
  FLASH_SALE
}

enum CampaignStatus {
  DRAFT
  SCHEDULED
  ACTIVE
  ENDED
}

model Campaign {
  id             String         @id @default(cuid())
  tenantId       String
  name           String
  type           CampaignType
  status         CampaignStatus @default(DRAFT)
  startDate      DateTime
  endDate        DateTime
  discount       Int? // Percentage discount
  targetAudience String // "All Customers", "Loyalty Members", etc
  description    String?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt

  tenant       Tenant        @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  landingHeros LandingHero[]

  @@index([tenantId])
  @@index([status])
  @@index([startDate])
  @@index([endDate])
  @@map("campaign")
}

model LandingHero {
  id          String   @id @default(cuid())
  tenantId    String
  campaignId  String? // Optional: Link to active campaign
  title       String
  subtitle    String
  description String
  ctaText     String
  ctaLink     String
  promoText   String?
  imageUrl    String
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  tenant   Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  campaign Campaign? @relation(fields: [campaignId], references: [id], onDelete: SetNull)

  @@index([tenantId])
  @@index([campaignId])
  @@map("landing_hero")
}

model SiteSettings {
  id       String @id @default(cuid())
  tenantId String
  key      String // 'header' or 'footer'

  // Header settings
  logoUrl  String?
  logoText String?

  // Social media links
  facebookUrl  String?
  instagramUrl String?
  twitterUrl   String?

  // Footer settings
  copyrightText String?
  footerLinks   Json? // JSON array of {label, url}

  updatedAt DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, key])
  @@index([tenantId])
  @@map("site_settings")
}

model SystemSettings {
  id                      String   @id @default(cuid())
  tenantId                String   @unique
  enableTax               Boolean  @default(false)
  taxRate                 Float    @default(0)
  taxName                 String   @default("PPN")
  currency                String   @default("IDR")
  timezone                String   @default("Asia/Jakarta")
  businessHours           Json? // Store opening hours
  enableInventoryTracking Boolean  @default(true)
  lowStockThreshold       Int      @default(10)
  enableReservations      Boolean  @default(true)
  enableOnlineOrdering    Boolean  @default(true)
  // Printer Settings
  enablePrinter           Boolean  @default(false)
  printerType             String   @default("EPSON_TM_T82") // EPSON_TM_T82, etc
  printerIpAddress        String?
  printerPort             Int      @default(9100)
  autoPrintReceipt        Boolean  @default(false)
  updatedAt               DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@map("system_settings")
}

// ============================================================================
// RBAC MODELS (Platform-managed Role-Based Access Control)
// ============================================================================

model Permission {
  id          String   @id @default(cuid())
  code        String   @unique // "MANAGE_MENU", "VIEW_ORDERS"
  name        String // "Manage Menu Items"
  description String
  category    String // "Menu", "Orders", "Settings", etc.
  isSystem    Boolean  @default(true) // System permissions cannot be deleted
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  rolePermissions           RolePermission[]
  businessTypePermissions   BusinessTypePermission[]
  tenantPermissionOverrides TenantPermissionOverride[]

  @@index([category])
  @@index([isSystem])
  @@map("permission")
}

model Role {
  id          String   @id @default(cuid())
  code        String   @unique // "OWNER", "MANAGER", "CASHIER"
  name        String // "Owner", "Manager"
  description String
  hierarchy   Int // 5=OWNER, 4=MANAGER, 3=CASHIER/WAITER/KITCHEN, 1=CUSTOMER
  isSystem    Boolean  @default(true) // System roles cannot be deleted
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  rolePermissions   RolePermission[]
  businessTypeRoles BusinessTypeRole[]

  @@index([hierarchy])
  @@index([isSystem])
  @@map("role")
}

model RolePermission {
  roleId       String
  permissionId String
  createdAt    DateTime @default(now())

  role       Role       @relation(fields: [roleId], references: [id], onDelete: Cascade)
  permission Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)

  @@id([roleId, permissionId])
  @@index([roleId])
  @@index([permissionId])
  @@map("role_permission")
}

model BusinessTypeRole {
  id           String   @id @default(cuid())
  businessType String // "RESTAURANT", "CAFE", "BAKERY"
  roleId       String
  isEnabled    Boolean  @default(true) // This business type uses this role
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  role Role @relation(fields: [roleId], references: [id], onDelete: Cascade)

  @@unique([businessType, roleId])
  @@index([businessType])
  @@index([roleId])
  @@map("business_type_role")
}

model BusinessTypePermission {
  id           String   @id @default(cuid())
  businessType String // "RESTAURANT", "CAFE", "BAKERY"
  permissionId String
  isEnabled    Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  permission Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)

  @@unique([businessType, permissionId])
  @@index([businessType])
  @@index([permissionId])
  @@map("business_type_permission")
}

model TenantPermissionOverride {
  id           String   @id @default(cuid())
  tenantId     String
  permissionId String
  roleCode     String // Which role this override applies to
  isGranted    Boolean // true = grant, false = revoke
  note         String? // Why this override was made
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant     Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  permission Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)

  @@unique([tenantId, permissionId, roleCode])
  @@index([tenantId])
  @@index([permissionId])
  @@map("tenant_permission_override")
}
